{% extends 'base.html.twig' %}

{% block title %}Nouvel étudiant{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center page-header">
        <h1>Créer un nouvel étudiant</h1>
    </div>

    <div class="card">
        <div class="card-header bg-white">
            <h5 class="card-title mb-0">Formulaire d'étudiant</h5>
        </div>
        <div class="card-body">
            {{ include('student/_form.html.twig') }}
        </div>
        <div class="card-footer bg-white">
            <a href="{{ path('app_student_index') }}" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-1"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
