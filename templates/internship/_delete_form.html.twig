<form method="post" action="{{ path('app_internship_delete', {'id': internship.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce stage ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ internship.id) }}">
    <button class="btn btn-danger">
        <i class="fas fa-trash-alt me-1"></i> Supprimer
    </button>
</form>
