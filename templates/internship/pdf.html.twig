<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Liste des stages</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            line-height: 1.6;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        h1 {
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>Liste des stages</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Date de début</th>
                <th>Date de fin</th>
            </tr>
        </thead>
        <tbody>
            {% for internship in internships %}
                <tr>
                    <td>{{ internship.id }}</td>
                    <td>{{ internship.title }}</td>
                    <td>{{ internship.description }}</td>
                    <td>{{ internship.startDate ? internship.startDate|date('d/m/Y') : 'Non défini' }}</td>
                    <td>{{ internship.endDate ? internship.endDate|date('d/m/Y') : 'Non défini' }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" style="text-align: center;">Aucun stage trouvé.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
