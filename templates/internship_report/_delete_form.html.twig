<form method="post" action="{{ path('app_internship_report_delete', {'id': intership_report_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intership_report_entity.id) }}">
    <button class="btn">Delete</button>
</form>
