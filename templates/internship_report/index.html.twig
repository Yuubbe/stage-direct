{% extends 'base.html.twig' %}

{% block title %}IntershipReportEntity index{% endblock %}

{% block body %}
    <h1>IntershipReportEntity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for intership_report_entity in intership_report_entities %}
            <tr>
                <td>{{ intership_report_entity.id }}</td>
                <td>
                    <a href="{{ path('app_internship_report_show', {'id': intership_report_entity.id}) }}">show</a>
                    <a href="{{ path('app_internship_report_edit', {'id': intership_report_entity.id}) }}">edit</a>
                    <a href="{{ path('app_internship_report_pdf', {'id': intership_report_entity.id}) }}" class="btn btn-sm btn-primary">
                        Download PDF
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_internship_report_new') }}">Create new</a>
{% endblock %}
