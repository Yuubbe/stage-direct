{% extends 'base.html.twig' %}

{% block title %}Nouvelle école{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center page-header">
        <h1>Créer une nouvelle école</h1>
    </div>

    <div class="card">
        <div class="card-header bg-white">
            <h5 class="card-title mb-0">Formulaire d'école</h5>
        </div>
        <div class="card-body">
            {{ include('school/_form.html.twig') }}
        </div>
        <div class="card-footer bg-white">
            <a href="{{ path('app_school_index') }}" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-1"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
